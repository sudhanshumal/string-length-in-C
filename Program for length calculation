#include <stdio.h>
int main()
{
    
    char s[1000];
    printf("Input a string to calculate length:\n");
    scanf("%s", &s);
    for(int i = 0; s[i] != '\0'; ++i);

    printf("Length of string: %d", i);
    return 0;
}
